<Window x:Class="CineFlix_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CineFlix_WPF"  
        mc:Ignorable="d"
        Title="CineFlix - Beheer" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <DockPanel>
        <local:HeaderControl DockPanel.Dock="Top"/>

        <!-- Menu (Jouw bestaande code) -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Bestand">
                <MenuItem Header="_Uitloggen" Click="LogoutMenuItem_Click"/>
                <Separator/>
                <MenuItem Header="_Afsluiten" Click="ExitMenuItem_Click"/>
            </MenuItem>
            <MenuItem Header="_Navigatie">
                <MenuItem Header="Films" Click="FilmsMenuItem_Click"/>
                <MenuItem Header="Regisseurs" Click="RegisseursMenuItem_Click"/>
                <MenuItem Header="Genres" Click="GenresMenuItem_Click"/>
            </MenuItem>
            <MenuItem Header="_Beheer" x:Name="AdminMenu" Visibility="Collapsed">
                <MenuItem Header="Gebruikers en Rollen" Click="UsersMenuItem_Click"/>
            </MenuItem>
        </Menu>

        <!-- Statusbalk (Jouw bestaande code) -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" Margin="5,0"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Hoofdinhoud met Tabs (Jouw bestaande code) -->
        <TabControl x:Name="MainTabControl">
            <!-- Films Tab -->
            <TabItem Header="Films">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Nieuwe Film" Click="AddFilmButton_Click" Margin="0,0,10,0"/>
                        <Label Content="Zoek op titel:" VerticalAlignment="Center"/>
                        <TextBox x:Name="FilmSearchBox" Width="250" TextChanged="FilmSearchBox_TextChanged"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" x:Name="FilmsDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Titel" Binding="{Binding Titel}" Width="2*"/>
                            <DataGridTextColumn Header="Jaar" Binding="{Binding Releasejaar}" Width="Auto"/>
                            <DataGridTextColumn Header="Regisseur" Binding="{Binding Regisseur.Naam}" Width="*"/>
                            <DataGridTextColumn Header="Rating" Binding="{Binding Rating}" Width="Auto"/>
                            <DataGridTemplateColumn Header="Acties" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="Bewerken" Margin="2" Click="EditFilmButton_Click" Tag="{Binding}"/>
                                            <Button Content="Verwijderen" Margin="2" Click="DeleteFilmButton_Click" Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Regisseurs Tab -->
            <TabItem Header="Regisseurs">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Nieuwe Regisseur" Click="AddRegisseurButton_Click" Margin="0,0,10,0"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" x:Name="RegisseursDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Naam" Binding="{Binding Naam}" Width="2*"/>
                            <DataGridTextColumn Header="Geboortejaar" Binding="{Binding Geboortejaar}" Width="*"/>
                            <DataGridTextColumn Header="Nationaliteit" Binding="{Binding Nationaliteit}" Width="*"/>
                            <DataGridTextColumn Header="Aantal Films" Binding="{Binding AantalFilms}" Width="*"/>
                            <DataGridTemplateColumn Header="Acties" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="Bewerken" Margin="2" Click="EditRegisseurButton_Click" Tag="{Binding}"/>
                                            <Button Content="Verwijderen" Margin="2" Click="DeleteRegisseurButton_Click" Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Genres Tab -->
            <TabItem Header="Genres">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Nieuw Genre" Click="AddGenreButton_Click" Margin="0,0,10,0"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" x:Name="GenresDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Genre" Binding="{Binding GenreNaam}" Width="*"/>
                            <DataGridTextColumn Header="Beschrijving" Binding="{Binding Beschrijving}" Width="2*"/>
                            <DataGridTextColumn Header="Aantal Films" Binding="{Binding AantalFilms}" Width="*"/>
                            <DataGridTemplateColumn Header="Acties" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="Bewerken" Margin="2" Click="EditGenreButton_Click" Tag="{Binding}"/>
                                            <Button Content="Verwijderen" Margin="2" Click="DeleteGenreButton_Click" Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Gebruikers Tab -->
            <TabItem x:Name="UsersTab" Header="Gebruikers" Visibility="Collapsed">
                <Grid Margin="10">
                    <DataGrid x:Name="UsersDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Naam" Binding="{Binding FullName}" Width="*"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                            <DataGridTextColumn Header="Registratiedatum" Binding="{Binding RegistrationDate, StringFormat='dd-MM-yyyy'}" Width="*"/>
                            <DataGridCheckBoxColumn Header="Geblokkeerd" Binding="{Binding IsDeleted}" Width="Auto"/>
                            <DataGridTemplateColumn Header="Acties" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="Blokkeren/Deblokkeren" Margin="2" Click="ToggleBlockUserButton_Click" Tag="{Binding}"/>
                                            <Button Content="Rollen Beheren" Margin="2" Click="ManageRolesButton_Click" Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>